
spring:
  datasource:
    url: jdbc:mysql://localhost:3303/talizin_store
    username: root
    password: 92470
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
  error:
    include-binding-errors: ON_PARAM